services:
  rick-bot:
    image: ghcr.io/voody2506/rick-bot:latest
    build: .
    env_file: .env
    volumes:
      - ./memory:/app/memory
      - ./work:/app/work
      - ./skills:/app/skills
      - ./tokens:/app/tokens
      - ${CLAUDE_HOME:-${HOME}/.claude}:/home/botuser/.claude:ro
      - ${CLAUDE_JSON:-${HOME}/.claude.json}:/home/botuser/.claude.json:ro
    restart: unless-stopped
